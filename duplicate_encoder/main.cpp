#include <string>
#include <vector>
#include <algorithm>

std::string duplicate_encoder(const std::string &word);

int main(){
    
    return 0;
}

std::string duplicate_encoder(const std::string &word)
{

    return "";
}